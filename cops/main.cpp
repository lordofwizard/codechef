#include <iostream>
#include <vector>
#include <algorighm>
using namespace std;

int main(){
	int t;
	cin >> t;
	while(t--){
		int num;
		cin >> num;
		int max_speed;
		cin >> max_speed;
		int max_time;
		cin >> max_time;
		int house[num];
		for(int i =0;i < num; i++){
			cin >> house[i];
		}
		// REAL LOGIC
		int distance = max_speed * max_time;
		
		std::vector<int> cops_see_house;

		for(int z=0;z<num;z++){
			for (int x=0;

		}
	
	}


	return 0;
}
